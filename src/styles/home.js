import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    main: {
        display: 'flex',
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    }
});